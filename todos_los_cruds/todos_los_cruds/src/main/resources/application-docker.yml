spring:
  application:
    name: sistema_completo

  datasource:
    url: jdbc:mysql://${MYSQLHOST:${SPRING_DATASOURCE_HOST:localhost}}:${MYSQLPORT:${SPRING_DATASOURCE_PORT:3306}}/${MYSQLDATABASE:${SPRING_DATASOURCE_DB:sistema_greentech}}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${MYSQLUSER:${SPRING_DATASOURCE_USERNAME:root}}
    password: ${MYSQLPASSWORD:${SPRING_DATASOURCE_PASSWORD:1234}}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false # ponlo en true si quieres ver las consultas
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: America/Bogota

  jackson:
    time-zone: America/Bogota

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  websocket:
    allowed-origins: "*"

  mail:
    host: smtp.gmail.com
    port: 587
    username: greentechonesystem@gmail.com
    password: akse nlfy lzun noun
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  address: 0.0.0.0
  port: ${PORT:8080}

app:
  frontend:
    url: ${APP_FRONTEND_URL:http://localhost:4200}

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: INFO
    com.api: INFO
